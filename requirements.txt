yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyarrow>=14.0        # parquet caching (setup_data.py / data.py)
quantstats>=0.0.62   # financial metrics + HTML tearsheet
matplotlib>=3.7      # fold timeline + confusion matrix plots
xgboost>=2.0         # optional, only needed once you switch model type to "xgboost"
